/** @type {World} */
let world;

/** @type {Config} */
let config;

/** Saved only to allow setting focus once the underlying canvas is ready. Seems necessary for openprocessing. */
let p5Canvas;

/**
 * I'm not sure if I want this to be part of "world" yet.  I _guess_ so.
 * @type {Editor} */
let editor;
